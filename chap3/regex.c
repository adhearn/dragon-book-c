#include <stdio.h>
#include <stdlib.h>

#define MAX_NUM_STATES 100

typedef struct State {

} State;

State **construct_nfa(char *regex) {
    int nextIndex = 0;
    State **states = malloc(MAX_NUM_STATES * sizeof(State));



    return states;
}

int main() {

}
